# go-oidc-server
Minimalist OpenID Connect (OIDC) Authorization Server built from scratch in Go. This project uses standard library only to explore OAuth 2.1 and OIDC Discovery fundamentals without external dependencies.
